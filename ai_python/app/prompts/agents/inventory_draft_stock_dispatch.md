# Playbook: inventory_draft — `stock_dispatch` (phase 2 — chưa triển khai)

Phiếu xuất kho thủ công sẽ dùng `inventoryId` (lô tồn) thay vì `skuCode`.  
**v1 chưa load file này** — chỉ dự phòng phase 2.
